import template from './newContact.html';
import controller from './newContact.controller';
import './newContact.scss';

let newContactComponent = {
  restrict: 'E',
  bindings: {},
  template,
  controller
};

export default newContactComponent;
